/**
 * @fileoverview Placeholder component retained for legacy imports
 * @note Version 0.1.0 no longer surfaces per-link navigation. This component exists
 *       solely to keep Next.js build toolchains happy while legacy bundles are pruned.
 */
export default function AuthenticatedNavLinks() {
  return null
}
